# Normalize to LF in the repo and working tree — these files are read inside
# Linux containers (Caddyfile, SQL, configs), where CRLF can break things.
* text=auto eol=lf

# Binary-safe defaults
*.png binary
*.jpg binary
*.ico binary
